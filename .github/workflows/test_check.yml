name: Base Test Check

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Run simple test
        run: |
          echo "Проверка структуры проекта..."
          ls -R
          echo "Все ок ✅"
