# Курс «Управление ИТ-проектами»

Добро пожаловать в репозиторий дисциплины. Этот проект является альтернативой Moodle и включает:
- лекции и Marp-слайды,
- практические задания,
- систему тестирования и оценивания,
- процесс сдачи через Pull Request и проверку преподавателем.

## Роли
- **Преподаватель (Maintainer)** — публикует задания, проверяет PR, ведёт Kanban.
- **Ассистент (Triage/Write)** — помогает проверять, комментировать, метить и переносить карточки.
- **Студент (Fork/Read)** — выполняет задания в форке и открывает PR в этот репозиторий.

## Как работать студенту
1) Сделать **fork** этого репозитория в свой аккаунт.  
2) Выполнить задание в `assignments/<lab*>` или в своей папке `students/<фамилия>/`.  
3) Создать **Pull Request** в ветку `main` этого репозитория.  
4) Дождаться комментариев и результата автопроверок (GitHub Actions).  

## Как работает проверка
- При каждом Pull Request запускается workflow `.github/workflows/test_check.yml`.  
- Результаты тестов прикрепляются как артефакт и отображаются в интерфейсе PR.  
- Преподаватель выставляет оценку в комментарии PR или в соответствующем Issue.

## Структура
- `lectures/` — лекции и слайды по темам.
- `assignments/` — задания, критерии оценивания и локальные тесты.
- `tests/` — общие тесты дисциплины (викторины, комбинированные проверки).
- `students/` — папки для индивидуальных отчётов (можно не использовать при форк-воркфлоу).
- `.github/` — шаблоны Issue/PR и автоматизация (Actions).


## Тестирование и экзамен
- В `tests/` есть тематические викторины и итоговый экзамен `final_exam/`.
- При PR запускаются автопроверки и (при настройке прав) формируется запись в `grades/grades.csv`.

## Авто-таблица оценок
Workflow `grades.yml` считывает данные PR и пополняет `grades/grades.csv`.  
Если у Actions есть права на запись (в настройках репозитория), изменения будут закоммичены в `main`.

## Линтеры и проверки структуры
- MarkdownLint проверяет корректность `.md` файлов.
- `check_structure.py` гарантирует наличие `report.md` в `students/<фамилия>/lab*/`.


## 🚀 Как развернуть курс

1. Создайте новый репозиторий в GitHub (публичный или приватный).
2. Загрузите содержимое архива или клонируйте и сделайте push:
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Управление ИТ-проектами v1.0.0"
   git branch -M main
   git remote add origin <url вашего репозитория>
   git push -u origin main
   git tag v1.0.0
   git push origin v1.0.0
   ```
3. Перейдите в **Settings → Actions → General → Workflow permissions** → выберите **Read and write**.
4. Проверьте работу Actions и откройте Project Board для визуализации.
5. Пригласите преподавателей как Maintainer и студентов через Fork.

**Готово!** Теперь курс функционирует как открытая образовательная платформа на GitHub.
